git remote -v
git remote remove origin
git remote add origin <URL_нового_репозитория_GitHub>

git restore --staged .
git reset --soft HEAD^
git reset --hard HEAD^

pytest -v -s --alluredir results
allure serve results
allure generate results

@pytest.mark.parametrize(
    "search_term, expected_result_count",
    [
        ("apple", 10),
        ("banana", 5),
        ("orange", 2),
    ],
    ids=lambda item: f"Search for {item[0]} (expected {item[1]})",  # Динамические имена
)